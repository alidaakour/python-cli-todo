# Python CLI To-Do

A simple command-line app to manage a list of items (add / list / remove) with JSON save/load.

## Features
- Add items
- List items with indexes
- Remove items by index (with validation)
- Save/Load from `data.json` (handles missing/broken file)

## Run
```bash
py main.py
